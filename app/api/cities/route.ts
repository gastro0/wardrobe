import {json,failure,ApiError,user} from "@/lib/server";
export async function GET(request:Request){try{user(request);const name=new URL(request.url).searchParams.get("q")?.trim()??"";if(name.length<2)return json({cities:[]});if(name.length>120)throw new ApiError(400,"Слишком длинное название города.");const url=new URL("https://geocoding-api.open-meteo.com/v1/search");url.search=new URLSearchParams({name,count:"8",language:"ru",format:"json"}).toString();const result=await fetch(url,{signal:AbortSignal.timeout(12000)});if(!result.ok)throw new ApiError(503,"Поиск городов временно недоступен. Попробуйте ещё раз.");const data:any=await result.json();return json({cities:(data.results??[]).map((c:any)=>({name:c.name,latitude:c.latitude,longitude:c.longitude,country:c.country,admin1:c.admin1}))})}catch(e){return failure(e)}}
