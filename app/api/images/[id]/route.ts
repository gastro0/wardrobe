import {db,bucket,user,failure,ApiError} from "@/lib/server";
export async function GET(request:Request,{params}:{params:Promise<{id:string}>}){try{const owner=user(request);const {id}=await params;const item=await db().prepare("SELECT image_key FROM wardrobe_items WHERE id=? AND user_id=?").bind(id,owner).first<{image_key:string}>();if(!item)throw new ApiError(404,"Фото не найдено.");const object=await bucket().get(item.image_key);if(!object)throw new ApiError(404,"Фото не найдено.");return new Response(object.body,{headers:{"Content-Type":object.httpMetadata?.contentType??"image/jpeg","Cache-Control":"private, no-store","X-Content-Type-Options":"nosniff"}})}catch(e){return failure(e)}}
